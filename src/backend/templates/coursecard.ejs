<!DOCTYPE html>
<html lang="en">
<!-- Course Cards -->
<div class="grid grid-cols-3 gap-6 mb-8">
    <% courses.forEach(function (course) { %>
    <a href="<%=course.course_name %>" class="block transition-colors duration-300 hover:bg-gray-100 rounded-lg">
        <div class="p-4 bg-white shadow-md rounded-lg">
            <div class="pt-4 pb-2">
                <h2 class="text-lg font-medium"><%=course.course_name%>></h2>
                <p class="text-gray-500"><%=course.description%></p>
            </div>
        </div>
    </a>
    <%}); %>
</div>
</html>